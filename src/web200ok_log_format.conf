log_format web200ok  '[$time_local] - $remote_addr - '
                     '$http_cf_connecting_ip | '
                     '$http_x_forwarded_for - "$request" '
                     '$status $body_bytes_sent "$http_referer" '
                     '"$http_user_agent" "$request_time"';